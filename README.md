![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![Pytest](https://img.shields.io/badge/Pytest-Test%20Framework-green)
![Allure](https://img.shields.io/badge/Allure-Reports-orange)
![API Testing](https://img.shields.io/badge/Testing-API%20Testing-brightgreen)

# Memes API Test Automation Framework
Проект автоматизации тестирования REST API для сервиса мемов. Включает полное покрытие CRUD операций и авторизации.

## 🚀 Функциональность:
Тестируемые endpoints:

- Авторизация (```/authorize```) - получение токена

- Создание мема (```POST /meme```) - добавление нового мема

- Чтение мемов (```GET /meme, GET /meme/{id}```) - получение всех мемов и одного мема по ID

- Обновление мема (```PUT /meme/{id}```) - полное обновление мема

- Удаление мема (```DELETE /meme/{id}```) - удаление мема

## Покрытие тестами:
- ✅ Позитивные сценарии

- ✅ Негативные сценарии

- ✅ Валидация данных

- ✅ Аутентификация и авторизация

- ✅ Проверка прав доступа (403 Forbidden)

- ✅ Обработка ошибок

## 🛠 Технологии
- Python 3.11+ - основной язык программирования

- Pytest - фреймворк для тестирования

- Requests - HTTP клиент для API запросов

- Pydantic - валидация данных

- Allure - генерация отчетов

- Pytest-xdist - параллельный запуск тестов

- Pytest-rerunfailures - повторное выполнение упавших тестов

## 📁 Структура проекта
```
meme_api_tests/
├── endpoints/              # Классы для работы с API endpoints
│   ├── __init__.py
│   ├── base_api.py        # Базовый класс API
│   ├── authorize.py       # Авторизация
│   ├── post_meme.py       # Создание мемов
│   ├── get_meme.py        # Получение мемов
│   ├── put_meme.py        # Обновление мемов
│   └── delete_meme.py     # Удаление мемов
├── schemas/               # Pydantic схемы для валидации
│   ├── __init__.py
│   ├── auth_schemas.py
│   ├── get_meme_schemas.py
│   ├── post_meme_schemas.py
│   └── put_meme_schemas.py
├── tests/                 # Тестовые сценарии
│   ├── test_authorization.py
│   ├── test_create_meme.py
│   ├── test_read_meme.py
│   ├── test_update_meme.py
│   └── test_delete_meme.py
├── conftest.py           # Pytest фикстуры
├── test_data.py          # Тестовые данные
├── pytest.ini           # Конфигурация Pytest
└── requirements.txt     # Зависимости проекта
```

## ⚙️ Установка и настройка

1. Клонирование репозитория
```
git clone https://github.com/kate-belova/memesapi_tests.git
```
2. Создание виртуального окружения
```
python -m venv .venv
source .venv/bin/activate  # Linux/MacOS
.venv\Scripts\activate     # Windows
```
3. Установка зависимостей
```
pip install -r requirements.txt
```
4. Установка Allure
```
# Windows (using scoop)
scoop install allure

# MacOS (using homebrew)
brew install allure

# Linux
sudo apt-add-repository ppa:qameta/allure
sudo apt-get update 
sudo apt-get install allure
```

## 🎯 Запуск тестов

Запуск всех тестов:
```
pytest
```
Запуск конкретной группы тестов:
```
# Только smoke тесты
pytest -m smoke

# Только тесты авторизации
pytest -m auth

# Только позитивные тесты
pytest -m positive

# Только негативные тесты
pytest -m negative
```
Запуск тестов конкретного модуля:
```
pytest tests/test_create_meme.py
pytest tests/test_read_meme.py
```

## 📊 Генерация Allure отчета:
Генерация отчета из результатов
```
allure serve allure-results
```
Генерация статического отчета:
```
allure generate allure-results -o allure-report --clean
```
Просмотр отчета:
```
allure open allure-report
```

## 🏷 Маркеры тестов:
```
| Маркер                     | Назначение              |
|-----------------------------|-------------------------|
| `@pytest.mark.smoke`        | Критичные smoke-тесты   |
| `@pytest.mark.positive`     | Позитивные сценарии     |
| `@pytest.mark.negative`     | Негативные сценарии     |
| `@pytest.mark.auth`         | Тесты авторизации       |
| `@pytest.mark.crud`         | Все CRUD операции       |
| `@pytest.mark.create`       | Создание мемов          |
| `@pytest.mark.read`         | Чтение мемов            |
| `@pytest.mark.fully_update` | Обновление мемов        |
| `@pytest.mark.delete`       | Удаление мемов          |
| `@pytest.mark.regression`   | Регрессионные тесты     |
| `@pytest.mark.memes`        | Все тесты мемов         |
```

## 📝 Тестовые данные
Пользователи для тестирования:
- Основной пользователь: ```kate```
- Другой пользователь: ```another_user```

## Типы тестовых данных:
- Валидные данные для создания мемов
- Невалидные данные (проверка ошибок)
- Данные для обновления мемов
- Данные авторизации

## Пример мема:
```
{
    "url": "https://t.me/python_community_rus/27/168416",
    "text": "Открыл книгу, а вот закрыть не получается..",
    "tags": ["it", "linux", "editors", "vim"],
    "info": {
        "group": "https://t.me/python_community_rus",
        "admin": "https://t.me/artemshumeiko"
    }
}
```

## 🔧 Конфигурация
Основные настройки в ```pytest.ini```:
- Параллельный запуск (4 потока)
- Автоматические ретраи упавших тестов (3 попытки)
- Интеграция с Allure
- Настройки маркеров

## 🐛 Troubleshooting
Частые проблемы и решения:
1. Ошибки подключения к API
  - Проверьте доступность ```http://memesapi.course.qa-practice.com```
  - Проверьте сетевые настройки

2. Проблемы с зависимостями
  - Обновите pip: ```pip install --upgrade pip```
  - Переустановите зависимости: ```pip install -r requirements.txt```

3. Проблемы с Allure
  - Убедитесь, что Allure установлен глобально
  - Проверьте переменные окружения PATH

4. Тесты падают с 403 ошибкой
  - Проверьте корректность токенов авторизации
  - Убедитесь, что тесты используют правильные headers

## 🤝 Вклад в проект
1. Форкните репозиторий
2. Создайте feature ветку: git checkout -b feature/amazing-feature
3. Закоммитьте изменения: git commit -m "Amazing feature added"
4. Запушьте ветку: git push origin feature/amazing-feature
5. Создайте Pull Request

## 👥 Контрибьюторы

- [**Евгений Окулик**](https://okulik.by/) — QA-инженер со стажем более 15 лет,  
  автор YouTube-канала [**Senior Tester**](https://www.youtube.com/@SeniorTester),  
  преподаватель автоматизации тестирования и разработчик **MemesAPI**.

- [**Екатерина Белова**](https://github.com/kate-belova) — автор данного фреймворка.
